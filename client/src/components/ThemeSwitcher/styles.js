import styled from '@emotion/styled';
import { Grid } from '@mui/material';
export const ThemeStyles = styled(Grid)({
    paddingRight: '10px',
    paddingTop: '10px',
    '& .themeIcon': {
        marginRight: '5px',
    },
});
